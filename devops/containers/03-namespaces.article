Namespaces
|| Last update: 5 April 2018

* Intro

.image images/namespaces.png

Example:

    docker run --net=host \
    -v /:/hostfs/ \
    --pid=host \
    --uts=host \
    --ipc=host \
    -v $(pwd):/src/ \
    alpine
